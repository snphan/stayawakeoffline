lowerBound = 100
upperBound = 200
endFlag = False

def processData(#bluetooth params passed????):
    #process data from muse
    return 150 #returns data from muse as single or tuples of frequencies

def main():
    #bluetooth connection stuff/lib
    
    while(True):
        while endFlag == False:

            curr = processData()

            if curr < lowerBound or curr > upperBound:
                #make beep sound

            #update End flag via GPIO/button
            
            #optional - delay timer for inner loop
            
        #optional - delay timer for outer loop
